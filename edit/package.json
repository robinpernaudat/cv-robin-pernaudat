{
  "name": "cv-robin-pernaudat",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "nginx:prepare": "mkdir -p .nginx/client_temp .nginx/proxy_temp .nginx/fastcgi_temp .nginx/uwsgi_temp .nginx/scgi_temp",
    "nginx:start": "nginx -p . -c nginx.local.conf",
    "nginx:stop": "nginx -p . -c nginx.local.conf -s stop"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17",
    "vite": "^5.4.14"
  }
}
